[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "devpilot"
version = "0.1.0"
description = "AI-assisted developer CLI for project intelligence and safe automation."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Harsh Nagar"}]

[project.scripts]
devpilot = "devpilot.cli:main"

[tool.setuptools.packages.find]
include = ["devpilot*"]
